{
  "name": "fusion-starter",
  "private": true,
  "type": "module",

  // --- ŞU BLOĞU (pkg) SİLiyoruz. Eğer bırakmak istersen en azından assets yolunu dist/** yapman gerekir. ---
  // "pkg": {
  //   "assets": ["dist/**/*"],
  //   "scripts": []
  // },

  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173"
  },

  "dependencies": {
    // (mevcut içeriğin)  örn: "react": "...", "react-dom": "...", "express": "...", "zod": "...", vb.
  },
  "devDependencies": {
    // (mevcut içeriğin)  örn: "vite": "...", "@vitejs/plugin-react-swc": "...", "tailwindcss": "...", vb.
  }
}
